import React from 'react';

const AddCarForm: React.FC = () => {
    return (
        <div>
            <h2>Add Car</h2>
            {/* Add car form here */}
        </div>
    );
};

export default AddCarForm;
